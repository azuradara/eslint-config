{
  "name": "@azuradara/eslint-config-mono",
  "private": true,
  "version": "0.1.1",
  "description": "ESLint config preset",
  "main": "index.js",
  "scripts": {
    "lint": "eslint .",
    "prepare": "pnpm -r run stub",
    "release": "bumpp package.json packages/*/package.json --commit --push --tag && pnpm -r publish --access public"
  },
  "keywords": [],
  "author": "Azura <dara-@outlook.jp> (https://github.com/azuradara)",
  "license": "MIT",
  "devDependencies": {
    "bumpp": "^8.2.1",
    "eslint": "^8.27.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.8.4"
  }
}
